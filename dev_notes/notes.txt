mz_zip_archive:
    struct for a zip file

mz_zip_writer_init_file:
    function to initialise zip file
    returns true for success

mz_zip_writer_add_mem:
    adds stuff I guess
    quite a long function, need to understand more on the individual arguments

mz_zip_writer_finalize_archive:
    finalises a zip file

mz_zip_writer_end:
    make the zip file?

MZ_BEST_COMPRESSION:
    ???